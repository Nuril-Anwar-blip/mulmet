name: tugas_akhir_bank
description: Bank Mandiri Mobile Banking App
publish_to: 'none'
version: 4.2.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  intl: ^0.18.1
  supabase_flutter: ^2.12.3
  crypto: ^3.0.2
  flutter_dotenv: ^5.1.0
  shared_preferences: ^2.2.3
  share_plus: ^10.0.2
  local_auth: ^2.3.0
  mobile_scanner: ^6.0.2
  fl_chart: ^0.69.0
  qr_flutter: ^4.1.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
